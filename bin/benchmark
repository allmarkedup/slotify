#!/usr/bin/env ruby

command = "bundle exec ruby performance/benchmark.rb"
targets = %i[action_view nice_partials view_component slotify]
target = ARGV.first

if target
  system("BENCHMARK_TARGET=#{target} #{command}")
else
  system(targets.map { "BENCHMARK_TARGET=#{_1} #{command}" }.join(";\n"))
end
